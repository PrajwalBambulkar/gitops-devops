replicaCount: 2

image:
  repository: prajwalbambulkar/backend
  tag: backend-10
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3000

containerPort: 3000
env:
  DB_HOST: mysql
  DB_USER: root
  DB_PASS: yourpassword
  DB_NAME: testdb
  DB_PORT: "3306"

