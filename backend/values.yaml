replicaCount: 2

image:
  repository: prajwalbambulkar/nodejs
  tag: "4"          # <-- Update this tag manually to promote to production
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3000

containerPort: 3000
