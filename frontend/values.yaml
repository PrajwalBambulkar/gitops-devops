replicaCount: 2

image:
  repository: prajwalbambulkar/frontend
  tag: frontend-15          # <-- Update manually to deploy new frontend build
  pullPolicy: IfNotPresent

service:
  type: NodePort
  port: 80

containerPort: 80
color: blue
activeColor: blue

