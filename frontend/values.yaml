replicaCount: 2

image:
  repository: prajwalbambulkar/3tiernodejs
  tag: frontend-34          # <-- Update manually to deploy new frontend build
  pullPolicy: IfNotPresent

service:
  type: NodePort
  port: 80

containerPort: 80
color: blue
activeColor: blue
hpa:
  enabled: true
resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "250m"
    memory: "256Mi"

